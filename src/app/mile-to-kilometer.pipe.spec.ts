import { MileToKilometerPipe } from './mile-to-kilometer.pipe';

describe('MileToKilometerPipe', () => {
  it('create an instance', () => {
    const pipe = new MileToKilometerPipe();
    expect(pipe).toBeTruthy();
  });
});
